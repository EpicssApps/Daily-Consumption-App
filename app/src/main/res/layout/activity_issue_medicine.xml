<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Vehicle -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Vehicle"
            android:textStyle="bold" />

        <Spinner
            android:id="@+id/spVehicle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:visibility="gone" />

        <TextView
            android:id="@+id/tvVehicleStatic"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#1Fffffff"
            android:padding="8dp"
            android:text="Vehicle"
            android:textColor="#ffffff"
            android:visibility="visible" />

        <!-- Medicine Picker -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Select Medicine"
            android:textStyle="bold" />

        <EditText
            android:id="@+id/edit_medicine"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clickable="true"
            android:drawableRight="@android:drawable/ic_menu_search"
            android:focusable="false"
            android:hint="Search / Select Medicine"
            android:inputType="none" />

        <!-- Quantity -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Issue Quantity"
            android:textStyle="bold" />

        <EditText
            android:id="@+id/etQty"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="e.g., 10"
            android:inputType="number" />

        <Button
            android:id="@+id/btnSave"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Save (Local)" />

        <!-- Pending Summary -->
        <TextView
            android:id="@+id/tvPendingTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:text="Pending items"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvPendingList"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="12dp"
            android:background="#1Fffffff"
            android:fadingEdgeLength="8dp"
            android:gravity="top|start"
            android:overScrollMode="always"
            android:padding="12dp"
            android:scrollbars="vertical"
            android:text="No pending items."
            android:textColor="#ffffff"
            android:textSize="14sp" />

        <Button
            android:id="@+id/btnManage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Delete or Edit Pending items" />

        <Button
            android:id="@+id/btnUpload"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Upload to Vehicle" />

        <!-- Daily Submit (FormActivity style) -->
        <Button
            android:id="@+id/btnSubmitRs01Consumption"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="Submit Day (RS-01)"
            android:visibility="gone" />

        <!-- Monthly (FormActivity style) -->
        <Button
            android:id="@+id/btnMonthlyFromStore"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Send RS-01 To Monthly Sheet"
            android:visibility="gone" />

    </LinearLayout>
</ScrollView>